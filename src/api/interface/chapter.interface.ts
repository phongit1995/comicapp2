export interface ChapterItem {
    commentCount: number,
    createdAt: string,
    index: number,
    images:string[],
    name: string,
    __v: number,
    _id: string,
    before:string,
    after:string
}